<Window x:Class="BankingApp.Views.CurrencyChangeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Change Currency" Height="200" Width="350" WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Name="CurrentCurrencyLabel" Grid.Row="0" Content="Current Currency: RON" Margin="0,5"/>
        
        <Label Grid.Row="1" Content="New Currency:" Margin="0,5"/>
        <ComboBox Name="NewCurrencyCombo" Grid.Row="1" Margin="0,30,0,5" SelectedIndex="0">
            <ComboBoxItem Content="RON"/>
            <ComboBoxItem Content="EUR"/>
            <ComboBoxItem Content="USD"/>
            <ComboBoxItem Content="GBP"/>
        </ComboBox>

        <TextBlock Grid.Row="2" Text="Note: A 1% fee will be applied." FontStyle="Italic" Margin="0,10" Foreground="Gray"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Change" Width="80" Height="30" Margin="5" Click="OnChangeClick"/>
            <Button Content="Cancel" Width="80" Height="30" Margin="5" Click="OnCancelClick"/>
        </StackPanel>
    </Grid>
</Window>
