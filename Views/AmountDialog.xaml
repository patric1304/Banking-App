<Window x:Class="BankingApp.Views.AmountDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Amount" Height="320" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Border CornerRadius="12" Background="White" Margin="15">
        <Border.Effect>
            <DropShadowEffect Color="#E2E8F0" BlurRadius="20" ShadowDepth="5" Opacity="0.4"/>
        </Border.Effect>
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" x:Name="TitleTextBlock" Text="Amount:" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,15"/>
            <TextBox Grid.Row="1" x:Name="AmountTextBox" FontSize="18" Margin="0,0,0,20"/>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Confirm" Width="120" Height="40" Click="OK_Click" Margin="0,0,10,0"/>
                <Button Content="Cancel" Width="100" Height="40" Click="Cancel_Click" Style="{StaticResource SecondaryButton}" Margin="0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
