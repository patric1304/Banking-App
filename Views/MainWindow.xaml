<Window x:Class="BankingApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Banking System" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FF2563EB" CornerRadius="12" Padding="25,20" Margin="0,0,0,25">
            <StackPanel>
                <TextBlock Text="🏦 Promise Banking System" 
                          FontSize="32" 
                          FontWeight="Bold" 
                          Foreground="White"
                          HorizontalAlignment="Center"/>
                <TextBlock Text="Manage your accounts with ease, keep your money safe" 
                          FontSize="16" 
                          Foreground="#FFE0E7FF"
                          FontWeight="SemiBold"
                          FontStyle="Italic"
                          HorizontalAlignment="Center"
                          Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Stats Panel -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="10" Padding="20" Margin="0,0,10,0">
                <Border.Effect>
                    <DropShadowEffect Color="#E2E8F0" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="Total Accounts" FontSize="14" Foreground="#64748B"/>
                    <TextBlock x:Name="TotalAccountsText" Text="0" FontSize="28" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="10" Padding="20" Margin="5,0">
                <Border.Effect>
                    <DropShadowEffect Color="#E2E8F0" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="Total Banks" FontSize="14" Foreground="#64748B"/>
                    <TextBlock x:Name="TotalBanksText" Text="0" FontSize="28" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="White" CornerRadius="10" Padding="20" Margin="10,0,0,0">
                <Border.Effect>
                    <DropShadowEffect Color="#E2E8F0" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="Quick Actions" FontSize="14" Foreground="#64748B"/>
                    <TextBlock Text="6 Available" FontSize="28" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Welcome Message / Account List -->
        <Grid Grid.Row="2" Margin="0,0,0,20">
            <!-- Welcome Message (shown when accounts are hidden) -->
            <Border x:Name="WelcomePanel" Background="White" CornerRadius="12" Padding="60,40" Margin="200,20" VerticalAlignment="Center" Visibility="Visible">
                <Border.Effect>
                    <DropShadowEffect Color="#E2E8F0" BlurRadius="20" ShadowDepth="5" Opacity="0.3"/>
                </Border.Effect>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="👋 Welcome to Promise Banking!" 
                              FontSize="34" 
                              FontWeight="Bold" 
                              Foreground="#2563EB"
                              HorizontalAlignment="Center"
                              Margin="0,0,0,10"
                              TextWrapping="Wrap"/>
                    <TextBlock Text="Your trusted partner in financial management" 
                              FontSize="16" 
                              Foreground="#64748B"
                              HorizontalAlignment="Center"
                              Margin="0,0,0,25"
                              TextWrapping="Wrap"/>
                    <Button x:Name="ViewAccountsButton" 
                           Content="👁️ View All Accounts" 
                           Width="200" 
                           Height="50" 
                           Click="ViewAccounts_Click" 
                           FontSize="15"
                           HorizontalAlignment="Center"
                           Margin="0"/>
                </StackPanel>
            </Border>

            <!-- Account List -->
            <GroupBox x:Name="AccountsGroupBox" Header="All Accounts" Margin="0,0,0,20" Visibility="Collapsed">
                <GroupBox.Effect>
                    <DropShadowEffect Color="#E2E8F0" BlurRadius="15" ShadowDepth="3" Opacity="0.3"/>
                </GroupBox.Effect>
                <ListView Name="AccountsListView" SelectionMode="Single" BorderThickness="0" FontSize="15">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Account Holder" Width="220">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding AccountHolder}" FontWeight="SemiBold" FontSize="15"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="IBAN" Width="250">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Iban}" FontFamily="Consolas" FontSize="14" Foreground="#475569"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Type" Width="130">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding AccountType}" FontSize="15"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Currency" Width="130">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding AccountCurrency}" FontWeight="Medium" FontSize="15" Foreground="#2563EB"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Balance" Width="180">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Amount, StringFormat='{}{0:N2}'}" FontWeight="Bold" FontSize="16" Foreground="#10B981"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </GroupBox>
        </Grid>

        <!-- Action Buttons -->
        <WrapPanel Grid.Row="3" HorizontalAlignment="Center">
            <Button Content="➕ Create Account" Width="160" Height="45" Click="CreateAccount_Click" Margin="5"/>
            <Button Content="💰 Deposit" Width="140" Height="45" Click="Deposit_Click" Margin="5"/>
            <Button Content="💸 Withdraw" Width="140" Height="45" Click="Withdraw_Click" Margin="5"/>
            <Button Content="💱 Change Currency" Width="180" Height="45" Click="ChangeCurrency_Click" Margin="5"/>
            <Button Content="📊 View Transactions" Width="190" Height="45" Click="ViewTransactions_Click" Margin="5"/>
            <Button Content="🗑️ Delete Account" Width="170" Height="45" Click="DeleteAccount_Click" Margin="5"/>
        </WrapPanel>
    </Grid>
</Window>